# Animation Assets

Drop the two homepage animation images in this folder using these filenames:

- `green-suit-charge.jpg`
- `start-line-sparks.jpg`

`index.html` is already wired to auto-show the new front-page animation cards when these files exist.

If you prefer `.png` or `.webp` instead, tell me the filenames and I can update the homepage references.
